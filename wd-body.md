This issue is the org's repo-conformance surface (backend#1608). The weekly
audit in `tracebloc/.github` (`caller-drift.yml`) rewrites this body on every
run; drift arrives separately as a **comment**. The body is the current
state, the comments are the history.

**Fail-closed contract:** the audit runs **Mondays 06:30 UTC**. If the
timestamp below is stale by more than ~8 days, the audit itself has died —
that staleness is the alarm, and fleet conformance is UNKNOWN rather than
fine. Do not close this issue.

---

### Last audit: 2026-08-12 07:09 UTC

- Repos evaluated: **20** (0 unreadable)
- Findings: **0**
- Verdict: ✅ **Conformant** — every repo read, every entry matched.

matrix

[Run log](https://x/1)
